# 参与贡献

欢迎提交问题反馈、文档改进、功能修复和平台适配。先阅读 [README](README.md) 了解功能，再根据工作内容查阅 [构建指南](docs/BUILD.md) 或 [模块开发说明](docs/PACKAGES.md)。

## 源码目录

| 目录 | 适合修改的内容 |
| --- | --- |
| `packages/` | 独立 npm 模块：OIDC、记忆、邮件、Studio 和共享成果服务。 |
| `dsh-plugins/`、`dsh-skills/`、`dsh-presets/` | 产品插件、任务指引与 Agent 预设。 |
| `dsh-host/` | 两种桌面壳共用的 Host、配置与系统能力边界。 |
| `dsh-electron/`、`dsh-desktop/` | Electron 主线与 Go/Wails 过渡壳。 |
| `config/`、`scripts/` | 通用发行配置、组件锁、构建和装配脚本。 |
| `tests/`、`docs/` | 产品检查、使用与维护文档；模块测试还保留在各自目录。 |

机构专属配置、插件和 skills 位于 EduWork-ECNU 的 `edition/`，由 `core.lock.json` 固定所复用的公版提交。共享能力应在公版修改，机构仓不复制公版实现。

## 提交修改

1. 从最新 main 创建主题分支，围绕一个明确问题修改代码或文档。功能使用 `feat/`，修复使用 `fix/`，文档使用 `docs/` 前缀；main 通过 PR 更新，不直接提交或推送。
2. 按受影响模块执行构建和必要的测试；文档修改运行 `node scripts/check-source-docs.mjs`。
3. 开发中的功能先提交 Draft PR。说明问题、修改后的行为和实际执行的验证；涉及界面时附演示截图。未验证项目如实列明。
4. 完成实现和本地验证后，将 PR 标记为待评审。维护者检查差异和现有 CI 结果，再合入 main；评审期间的修改继续提交到同一分支。

多个任务同时开发时，优先使用独立 worktree，避免把无关的未提交文件一起带入 PR。不要强推 main 或把内部讨论、机器配置和真实用户测试数据提交到功能分支；公开分支同样属于公开源码。

合并 PR 不会自动批准发行。npm 发布、产品依赖锁更新、桌面 Release 与机构部署按各自流程执行；版本号和 Release notes 仍须由维护者确认。

涉及公共接口的新功能先写清楚兼容边界和验收条件。示例：[双协议网关认证提案](docs/proposals/gateway-auth/README.md)。提案不代表已发布的功能或可直接使用的配置。

对话与 Studio 共用生成、预览和下载实现。通用功能在 EduWork 维护；机构服务通过配置或扩展接入，公版不能依赖机构凭据、私有网络或相邻源码仓库。

## 文档与数据

README 默认使用中文，`README_EN.md` 提供英文介绍，两者保持一致。首页介绍功能、安装与使用，具体配置、接口和开发方法放在对应文档中。

提交到仓库的文档应对用户或贡献者有用。内部讨论、任务交接、个人工作记录、本地验收流水账和账户操作记录留在仓库外；不将它们作为公开文档或链接入口。使用合成数据和占位配置，勿提交用户会话、诊断包、真实 Client ID、密钥、机器路径或构建缓存。

## 测试与发行

普通 CI 检查受影响的源码、依赖和构建。真实模型、企业登录、Office、音视频和桌面行为按改动范围在本地验证；PR 中说明未验证的范围。Fork PR 不应获得发布凭据。

公共模块保持独立 npm 版本。先发布并核验 npm 包，再更新产品的精确依赖锁；修改模块源码不会自动改变桌面包。发行流程见 [包维护](docs/PACKAGES.md) 和 [产品发行](docs/RELEASE.md)。

macOS 贡献请阅读 [平台适配说明](docs/MACOS.md)，记录实际测试的硬件、系统与架构。Windows 或跨平台依赖安装成功不能代替 macOS 实机验证。
